# ── Dependencies ─────────────────────────────────
node_modules/

# ── Environment (NEVER commit real keys) ─────────
.env
.env.local
.env.production
.env.production.local
.env.development.local

# ── Build output ──────────────────────────────────
dist/
build/

# ── OS / editor ───────────────────────────────────
.DS_Store
Thumbs.db
*.log
*.log.*
.vscode/
.idea/

# ── Render / Vercel cache ─────────────────────────
.vercel/
.render/

# -- Python --
__pycache__/
*.pyc
*.pyo
.pytest_cache/
venv/
.venv/
env/
*.egg-info/

# -- Streamlit (KA017 legacy) --
.streamlit/secrets.toml

# -- Local DB caches (SQLite/Turso) --
*.db-wal
*.db-shm

# -- KA017 runtime data (local, not source) --
python-backend/data/
