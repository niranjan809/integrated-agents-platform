# ── Copy this file to .env and fill in your values ──
# .env is gitignored — never commit real keys

# RapidAPI keys (twitter-api45 on rapidapi.com)
RAPIDAPI_KEY=your_rapidapi_key_here
RAPIDAPI_KEY_BACKUP=your_backup_rapidapi_key_here
# Paid shared key — set PAID_KEY_RPM to a conservative value (2-4) if others share this key
RAPIDAPI_KEY_PAID=your_paid_rapidapi_key_here
PAID_KEY_RPM=3
RAPIDAPI_HOST=twitter-api45.p.rapidapi.com
PORT=3001

# Turso Database (your own DB)
TURSO_URL=libsql://your-db-name.region.turso.io
TURSO_TOKEN=your_turso_auth_token_here

# JWT — use a long random string in production
JWT_SECRET=change-this-to-a-long-random-string-in-production

# Admin credentials — auto-created on startup
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=YourStrongPassword123!

# OpenRouter API (get key from openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Friend's Turso DB — READ-ONLY keyword source (never written to)
FRIEND_TURSO_URL=libsql://friends-db-name.region.turso.io
FRIEND_TURSO_TOKEN=friends_turso_auth_token_here
FRIEND_KEYWORDS_TABLE=keywords
FRIEND_KEYWORDS_COLUMN=keyword

# Allowed frontend origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173
